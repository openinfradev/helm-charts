{{- if .Values.machinePool }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "cluster-api-aws.fullname" . }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "5"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      name: {{ include "cluster-api-aws.fullname" . }}
    spec:
      serviceAccountName: {{ include "cluster-api-aws.serviceAccountName" . }}
      containers:
      - name: generate-mp
        image: "{{ .Values.job.image.repository }}:{{ .Values.job.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.job.image.pullPolicy }}
        command:
        - /generate_machine_pool.py  
        - {{ .Values.cluster.name }}
        volumeMounts:
        - name: vol
          subPath: mp.raw.yaml
          mountPath: /mp.raw.yaml
        - name: vol
          subPath: generate_machine_pool.py
          mountPath: /generate_machine_pool.py

      {{- if .Values.job.taconode.enabled }}
      - name: wait-and-initialize
        image: "{{ .Values.job.image.repository }}:{{ .Values.job.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.job.image.pullPolicy }}
        command:
        - /wait_and_initialize.py
        - {{ .Values.cluster.name }}
        - taco
        volumeMounts:
        - name: kubeconfig
          subPath: value
          mountPath: /kube.config
        - name: vol
          subPath: wait_and_initialize.py
          mountPath: /wait_and_initialize.py
      {{- end }}

      {{- if .Values.job.argo.enabled }}
      - name: register-argo
        image: "{{ .Values.job.image.repository }}:{{ .Values.job.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.job.image.pullPolicy }}
        command:
        - /argo-register.sh
        - {{.Values.job.argo.url }}
        - {{.Values.job.argo.user }}
        - {{.Values.job.argo.password }}
        - {{.Values.cluster.name }}

        volumeMounts:
        - name: kubeconfig
          subPath: value
          mountPath: /kube.config
        - name: vol
          subPath: argo-register.sh
          mountPath: /argo-register.sh
      {{- end }}

      volumes:
      - name: kubeconfig
        secret:
          secretName: {{ .Values.cluster.name }}-kubeconfig
      - name: vol
        configMap:
          name: {{ include "cluster-api-aws.fullname" . }}
          defaultMode: 0555
      restartPolicy: OnFailure
{{- end}}
