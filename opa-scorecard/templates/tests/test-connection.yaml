apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opa-scorecard.fullname" . }}-test-connection"
  labels:
    {{- include "opa-scorecard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opa-scorecard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
